-- =============================================================================
-- H2 デモ環境用初期データ
-- Seed データは SeedRunner によりアプリケーション起動時に投入されます
-- =============================================================================

-- ダミー文（Spring SQL init は空スクリプトを許容しないため）
SELECT 1;
