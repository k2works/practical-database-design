<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pms.infrastructure.out.persistence.mapper.SupplierMapper">

    <resultMap id="SupplierResultMap" type="com.example.pms.domain.model.supplier.Supplier">
        <id property="supplierCode" column="取引先コード"/>
        <id property="effectiveFrom" column="適用開始日"/>
        <result property="effectiveTo" column="適用停止日"/>
        <result property="supplierName" column="取引先名"/>
        <result property="supplierNameKana" column="取引先カナ"/>
        <result property="supplierType" column="取引先区分"
                typeHandler="com.example.pms.infrastructure.out.persistence.typehandler.SupplierTypeTypeHandler"/>
        <result property="postalCode" column="郵便番号"/>
        <result property="address" column="住所"/>
        <result property="phoneNumber" column="電話番号"/>
        <result property="faxNumber" column="FAX番号"/>
        <result property="contactPerson" column="担当者名"/>
        <result property="createdAt" column="作成日時"/>
        <result property="updatedAt" column="更新日時"/>
    </resultMap>

    <!-- PostgreSQL用 INSERT -->
    <insert id="insert" parameterType="com.example.pms.domain.model.supplier.Supplier" databaseId="postgresql">
        INSERT INTO "取引先マスタ" (
            "取引先コード", "適用開始日", "適用停止日", "取引先名", "取引先カナ",
            "取引先区分", "郵便番号", "住所", "電話番号", "FAX番号", "担当者名",
            "作成日時", "更新日時"
        ) VALUES (
            #{supplierCode}, #{effectiveFrom}, #{effectiveTo}, #{supplierName}, #{supplierNameKana},
            #{supplierType, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.SupplierTypeTypeHandler}::取引先区分,
            #{postalCode}, #{address}, #{phoneNumber}, #{faxNumber}, #{contactPerson},
            CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
        )
    </insert>

    <!-- H2用 INSERT -->
    <insert id="insert" parameterType="com.example.pms.domain.model.supplier.Supplier" databaseId="h2">
        INSERT INTO "取引先マスタ" (
            "取引先コード", "適用開始日", "適用停止日", "取引先名", "取引先カナ",
            "取引先区分", "郵便番号", "住所", "電話番号", "FAX番号", "担当者名",
            "作成日時", "更新日時"
        ) VALUES (
            #{supplierCode}, #{effectiveFrom}, #{effectiveTo}, #{supplierName}, #{supplierNameKana},
            #{supplierType, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.SupplierTypeTypeHandler},
            #{postalCode}, #{address}, #{phoneNumber}, #{faxNumber}, #{contactPerson},
            CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
        )
    </insert>

    <select id="findBySupplierCode" resultMap="SupplierResultMap">
        SELECT * FROM "取引先マスタ"
        WHERE "取引先コード" = #{supplierCode}
        ORDER BY "適用開始日" DESC
        LIMIT 1
    </select>

    <select id="findBySupplierCodeAndDate" resultMap="SupplierResultMap">
        SELECT * FROM "取引先マスタ"
        WHERE "取引先コード" = #{supplierCode}
          AND "適用開始日" &lt;= #{baseDate}
          AND ("適用停止日" IS NULL OR "適用停止日" > #{baseDate})
        ORDER BY "適用開始日" DESC
        LIMIT 1
    </select>

    <select id="findAll" resultMap="SupplierResultMap">
        SELECT * FROM "取引先マスタ"
        ORDER BY "取引先コード", "適用開始日" DESC
    </select>

    <!-- PostgreSQL用 UPDATE -->
    <update id="update" parameterType="com.example.pms.domain.model.supplier.Supplier" databaseId="postgresql">
        UPDATE "取引先マスタ" SET
            "取引先名" = #{supplierName},
            "取引先カナ" = #{supplierNameKana},
            "取引先区分" = #{supplierType, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.SupplierTypeTypeHandler}::取引先区分,
            "郵便番号" = #{postalCode},
            "住所" = #{address},
            "電話番号" = #{phoneNumber},
            "FAX番号" = #{faxNumber},
            "担当者名" = #{contactPerson},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "取引先コード" = #{supplierCode}
          AND "適用開始日" = #{effectiveFrom}
    </update>

    <!-- H2用 UPDATE -->
    <update id="update" parameterType="com.example.pms.domain.model.supplier.Supplier" databaseId="h2">
        UPDATE "取引先マスタ" SET
            "取引先名" = #{supplierName},
            "取引先カナ" = #{supplierNameKana},
            "取引先区分" = #{supplierType, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.SupplierTypeTypeHandler},
            "郵便番号" = #{postalCode},
            "住所" = #{address},
            "電話番号" = #{phoneNumber},
            "FAX番号" = #{faxNumber},
            "担当者名" = #{contactPerson},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "取引先コード" = #{supplierCode}
          AND "適用開始日" = #{effectiveFrom}
    </update>

    <!-- PostgreSQL用 DELETE -->
    <delete id="deleteAll" databaseId="postgresql">
        TRUNCATE TABLE "取引先マスタ" CASCADE
    </delete>

    <!-- H2用 DELETE -->
    <delete id="deleteAll" databaseId="h2">
        DELETE FROM "取引先マスタ"
    </delete>
</mapper>
