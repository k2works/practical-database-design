<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pms.infrastructure.out.persistence.mapper.ItemMapper">

    <!-- 日本語カラム名と英語プロパティ名のマッピング -->
    <resultMap id="ItemResultMap" type="com.example.pms.domain.model.item.Item">
        <id property="id" column="ID"/>
        <result property="itemCode" column="品目コード"/>
        <result property="effectiveFrom" column="適用開始日"/>
        <result property="effectiveTo" column="適用停止日"/>
        <result property="itemName" column="品名"/>
        <result property="itemCategory" column="品目区分"
                typeHandler="com.example.pms.infrastructure.out.persistence.typehandler.ItemCategoryTypeHandler"/>
        <result property="unitCode" column="単位コード"/>
        <result property="leadTime" column="リードタイム"/>
        <result property="safetyLeadTime" column="安全リードタイム"/>
        <result property="safetyStock" column="安全在庫数"/>
        <result property="yieldRate" column="歩留率"/>
        <result property="minLotSize" column="最小ロット数"/>
        <result property="lotIncrement" column="刻みロット数"/>
        <result property="maxLotSize" column="最大ロット数"/>
        <result property="shelfLife" column="有効期間"/>
        <result property="createdAt" column="作成日時"/>
        <result property="updatedAt" column="更新日時"/>
    </resultMap>

    <!-- PostgreSQL用 INSERT -->
    <insert id="insert" parameterType="com.example.pms.domain.model.item.Item"
            useGeneratedKeys="true" keyProperty="id" keyColumn="ID" databaseId="postgresql">
        INSERT INTO "品目マスタ" (
            "品目コード",
            "適用開始日",
            "適用停止日",
            "品名",
            "品目区分",
            "単位コード",
            "リードタイム",
            "安全リードタイム",
            "安全在庫数",
            "歩留率",
            "最小ロット数",
            "刻みロット数",
            "最大ロット数",
            "有効期間",
            "作成日時",
            "更新日時"
        ) VALUES (
            #{itemCode},
            #{effectiveFrom},
            #{effectiveTo},
            #{itemName},
            #{itemCategory, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.ItemCategoryTypeHandler}::品目区分,
            #{unitCode},
            #{leadTime},
            #{safetyLeadTime},
            #{safetyStock},
            #{yieldRate},
            #{minLotSize},
            #{lotIncrement},
            #{maxLotSize},
            #{shelfLife},
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP
        )
    </insert>

    <!-- H2用 INSERT -->
    <insert id="insert" parameterType="com.example.pms.domain.model.item.Item"
            useGeneratedKeys="true" keyProperty="id" keyColumn="ID" databaseId="h2">
        INSERT INTO "品目マスタ" (
            "品目コード",
            "適用開始日",
            "適用停止日",
            "品名",
            "品目区分",
            "単位コード",
            "リードタイム",
            "安全リードタイム",
            "安全在庫数",
            "歩留率",
            "最小ロット数",
            "刻みロット数",
            "最大ロット数",
            "有効期間",
            "作成日時",
            "更新日時"
        ) VALUES (
            #{itemCode},
            #{effectiveFrom},
            #{effectiveTo},
            #{itemName},
            #{itemCategory, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.ItemCategoryTypeHandler},
            #{unitCode},
            #{leadTime},
            #{safetyLeadTime},
            #{safetyStock},
            #{yieldRate},
            #{minLotSize},
            #{lotIncrement},
            #{maxLotSize},
            #{shelfLife},
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP
        )
    </insert>

    <select id="findByItemCode" resultMap="ItemResultMap">
        SELECT * FROM "品目マスタ"
        WHERE "品目コード" = #{itemCode}
        ORDER BY "適用開始日" DESC
        LIMIT 1
    </select>

    <select id="findByItemCodeAndDate" resultMap="ItemResultMap">
        SELECT * FROM "品目マスタ"
        WHERE "品目コード" = #{itemCode}
          AND "適用開始日" &lt;= #{baseDate}
          AND ("適用停止日" IS NULL OR "適用停止日" > #{baseDate})
        ORDER BY "適用開始日" DESC
        LIMIT 1
    </select>

    <select id="findAll" resultMap="ItemResultMap">
        SELECT * FROM "品目マスタ"
        ORDER BY "品目コード", "適用開始日" DESC
    </select>

    <!-- PostgreSQL用 UPDATE -->
    <update id="update" parameterType="com.example.pms.domain.model.item.Item" databaseId="postgresql">
        UPDATE "品目マスタ" SET
            "品名" = #{itemName},
            "品目区分" = #{itemCategory, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.ItemCategoryTypeHandler}::品目区分,
            "単位コード" = #{unitCode},
            "リードタイム" = #{leadTime},
            "安全リードタイム" = #{safetyLeadTime},
            "安全在庫数" = #{safetyStock},
            "歩留率" = #{yieldRate},
            "最小ロット数" = #{minLotSize},
            "刻みロット数" = #{lotIncrement},
            "最大ロット数" = #{maxLotSize},
            "有効期間" = #{shelfLife},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "ID" = #{id}
    </update>

    <!-- H2用 UPDATE -->
    <update id="update" parameterType="com.example.pms.domain.model.item.Item" databaseId="h2">
        UPDATE "品目マスタ" SET
            "品名" = #{itemName},
            "品目区分" = #{itemCategory, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.ItemCategoryTypeHandler},
            "単位コード" = #{unitCode},
            "リードタイム" = #{leadTime},
            "安全リードタイム" = #{safetyLeadTime},
            "安全在庫数" = #{safetyStock},
            "歩留率" = #{yieldRate},
            "最小ロット数" = #{minLotSize},
            "刻みロット数" = #{lotIncrement},
            "最大ロット数" = #{maxLotSize},
            "有効期間" = #{shelfLife},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "ID" = #{id}
    </update>

    <!-- PostgreSQL用 DELETE -->
    <delete id="deleteAll" databaseId="postgresql">
        TRUNCATE TABLE "品目マスタ" CASCADE
    </delete>

    <!-- H2用 DELETE -->
    <delete id="deleteAll" databaseId="h2">
        DELETE FROM "品目マスタ"
    </delete>
</mapper>
