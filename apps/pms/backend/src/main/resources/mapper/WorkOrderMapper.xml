<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pms.infrastructure.out.persistence.mapper.WorkOrderMapper">

    <resultMap id="WorkOrderResultMap" type="com.example.pms.domain.model.process.WorkOrder">
        <id property="id" column="ID"/>
        <result property="workOrderNumber" column="作業指示番号"/>
        <result property="orderNumber" column="オーダ番号"/>
        <result property="workOrderDate" column="作業指示日"/>
        <result property="itemCode" column="品目コード"/>
        <result property="orderQuantity" column="作業指示数"/>
        <result property="locationCode" column="場所コード"/>
        <result property="plannedStartDate" column="開始予定日"/>
        <result property="plannedEndDate" column="完成予定日"/>
        <result property="actualStartDate" column="実績開始日"/>
        <result property="actualEndDate" column="実績完了日"/>
        <result property="completedQuantity" column="完成済数"/>
        <result property="totalGoodQuantity" column="総良品数"/>
        <result property="totalDefectQuantity" column="総不良品数"/>
        <result property="status" column="ステータス"
                typeHandler="com.example.pms.infrastructure.out.persistence.typehandler.WorkOrderStatusTypeHandler"/>
        <result property="completedFlag" column="完了フラグ"/>
        <result property="remarks" column="備考"/>
        <result property="createdAt" column="作成日時"/>
        <result property="createdBy" column="作成者"/>
        <result property="updatedAt" column="更新日時"/>
        <result property="updatedBy" column="更新者"/>
        <result property="version" column="バージョン"/>
    </resultMap>

    <!-- 明細を含むResultMap（nested select） -->
    <resultMap id="WorkOrderWithDetailsResultMap" type="com.example.pms.domain.model.process.WorkOrder"
               extends="WorkOrderResultMap">
        <collection property="details" ofType="com.example.pms.domain.model.process.WorkOrderDetail"
                    column="作業指示番号" select="com.example.pms.infrastructure.out.persistence.mapper.WorkOrderDetailMapper.findByWorkOrderNumber"/>
    </resultMap>

    <!-- PostgreSQL用 INSERT -->
    <insert id="insert" parameterType="com.example.pms.domain.model.process.WorkOrder"
            useGeneratedKeys="true" keyProperty="id" keyColumn="ID" databaseId="postgresql">
        INSERT INTO "作業指示データ" (
            "作業指示番号", "オーダ番号", "作業指示日", "品目コード", "作業指示数",
            "場所コード", "開始予定日", "完成予定日", "完成済数", "総良品数", "総不良品数",
            "ステータス", "完了フラグ", "備考", "作成者", "更新者", "バージョン"
        ) VALUES (
            #{workOrderNumber},
            #{orderNumber},
            #{workOrderDate},
            #{itemCode},
            #{orderQuantity},
            #{locationCode},
            #{plannedStartDate},
            #{plannedEndDate},
            #{completedQuantity},
            #{totalGoodQuantity},
            #{totalDefectQuantity},
            #{status, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.WorkOrderStatusTypeHandler}::作業指示ステータス,
            #{completedFlag},
            #{remarks},
            #{createdBy},
            #{updatedBy},
            #{version}
        )
    </insert>

    <!-- H2用 INSERT -->
    <insert id="insert" parameterType="com.example.pms.domain.model.process.WorkOrder"
            useGeneratedKeys="true" keyProperty="id" keyColumn="ID" databaseId="h2">
        INSERT INTO "作業指示データ" (
            "作業指示番号", "オーダ番号", "作業指示日", "品目コード", "作業指示数",
            "場所コード", "開始予定日", "完成予定日", "完成済数", "総良品数", "総不良品数",
            "ステータス", "完了フラグ", "備考", "作成者", "更新者", "バージョン"
        ) VALUES (
            #{workOrderNumber},
            #{orderNumber},
            #{workOrderDate},
            #{itemCode},
            #{orderQuantity},
            #{locationCode},
            #{plannedStartDate},
            #{plannedEndDate},
            #{completedQuantity},
            #{totalGoodQuantity},
            #{totalDefectQuantity},
            #{status, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.WorkOrderStatusTypeHandler},
            #{completedFlag},
            #{remarks},
            #{createdBy},
            #{updatedBy},
            #{version}
        )
    </insert>

    <select id="findById" resultMap="WorkOrderResultMap">
        SELECT * FROM "作業指示データ" WHERE "ID" = #{id}
    </select>

    <select id="findByWorkOrderNumber" resultMap="WorkOrderResultMap">
        SELECT * FROM "作業指示データ" WHERE "作業指示番号" = #{workOrderNumber}
    </select>

    <!-- 明細を含めて取得 -->
    <select id="findByWorkOrderNumberWithDetails" resultMap="WorkOrderWithDetailsResultMap">
        SELECT * FROM "作業指示データ" WHERE "作業指示番号" = #{workOrderNumber}
    </select>

    <select id="findByOrderNumber" resultMap="WorkOrderResultMap">
        SELECT * FROM "作業指示データ" WHERE "オーダ番号" = #{orderNumber} ORDER BY "作業指示日" DESC
    </select>

    <!-- PostgreSQL用 findByStatus -->
    <select id="findByStatus" resultMap="WorkOrderResultMap" databaseId="postgresql">
        SELECT * FROM "作業指示データ"
        WHERE "ステータス" = #{status, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.WorkOrderStatusTypeHandler}::作業指示ステータス
        ORDER BY "作業指示日" DESC
    </select>

    <!-- H2用 findByStatus -->
    <select id="findByStatus" resultMap="WorkOrderResultMap" databaseId="h2">
        SELECT * FROM "作業指示データ"
        WHERE "ステータス" = #{status, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.WorkOrderStatusTypeHandler}
        ORDER BY "作業指示日" DESC
    </select>

    <select id="findAll" resultMap="WorkOrderResultMap">
        SELECT * FROM "作業指示データ" ORDER BY "作業指示日" DESC
    </select>

    <!-- ページネーション付き検索 -->
    <select id="findWithPagination" resultMap="WorkOrderResultMap">
        SELECT * FROM "作業指示データ"
        <where>
            <if test="keyword != null and keyword != ''">
                ("作業指示番号" LIKE '%' || #{keyword} || '%'
                OR "オーダ番号" LIKE '%' || #{keyword} || '%'
                OR "品目コード" LIKE '%' || #{keyword} || '%'
                OR "場所コード" LIKE '%' || #{keyword} || '%'
                OR "備考" LIKE '%' || #{keyword} || '%')
            </if>
        </where>
        ORDER BY "作業指示日" DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 件数取得 -->
    <select id="count" resultType="long">
        SELECT COUNT(*) FROM "作業指示データ"
        <where>
            <if test="keyword != null and keyword != ''">
                ("作業指示番号" LIKE '%' || #{keyword} || '%'
                OR "オーダ番号" LIKE '%' || #{keyword} || '%'
                OR "品目コード" LIKE '%' || #{keyword} || '%'
                OR "場所コード" LIKE '%' || #{keyword} || '%'
                OR "備考" LIKE '%' || #{keyword} || '%')
            </if>
        </where>
    </select>

    <!-- PostgreSQL用 UPDATE -->
    <update id="update" parameterType="com.example.pms.domain.model.process.WorkOrder" databaseId="postgresql">
        UPDATE "作業指示データ" SET
            "オーダ番号" = #{orderNumber},
            "作業指示日" = #{workOrderDate},
            "品目コード" = #{itemCode},
            "作業指示数" = #{orderQuantity},
            "場所コード" = #{locationCode},
            "開始予定日" = #{plannedStartDate},
            "完成予定日" = #{plannedEndDate},
            "実績開始日" = #{actualStartDate},
            "実績完了日" = #{actualEndDate},
            "完成済数" = #{completedQuantity},
            "総良品数" = #{totalGoodQuantity},
            "総不良品数" = #{totalDefectQuantity},
            "ステータス" = #{status, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.WorkOrderStatusTypeHandler}::作業指示ステータス,
            "完了フラグ" = #{completedFlag},
            "備考" = #{remarks},
            "更新日時" = CURRENT_TIMESTAMP,
            "更新者" = #{updatedBy},
            "バージョン" = "バージョン" + 1
        WHERE "作業指示番号" = #{workOrderNumber}
    </update>

    <!-- H2用 UPDATE -->
    <update id="update" parameterType="com.example.pms.domain.model.process.WorkOrder" databaseId="h2">
        UPDATE "作業指示データ" SET
            "オーダ番号" = #{orderNumber},
            "作業指示日" = #{workOrderDate},
            "品目コード" = #{itemCode},
            "作業指示数" = #{orderQuantity},
            "場所コード" = #{locationCode},
            "開始予定日" = #{plannedStartDate},
            "完成予定日" = #{plannedEndDate},
            "実績開始日" = #{actualStartDate},
            "実績完了日" = #{actualEndDate},
            "完成済数" = #{completedQuantity},
            "総良品数" = #{totalGoodQuantity},
            "総不良品数" = #{totalDefectQuantity},
            "ステータス" = #{status, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.WorkOrderStatusTypeHandler},
            "完了フラグ" = #{completedFlag},
            "備考" = #{remarks},
            "更新日時" = CURRENT_TIMESTAMP,
            "更新者" = #{updatedBy},
            "バージョン" = "バージョン" + 1
        WHERE "作業指示番号" = #{workOrderNumber}
    </update>

    <!-- DELETE BY WORK ORDER NUMBER -->
    <delete id="deleteByWorkOrderNumber">
        DELETE FROM "作業指示データ" WHERE "作業指示番号" = #{workOrderNumber}
    </delete>

    <!-- 完成数量更新（楽観ロック対応） -->
    <update id="updateCompletionQuantities">
        UPDATE "作業指示データ" SET
            "完成済数" = "完成済数" + #{completedQuantity},
            "総良品数" = "総良品数" + #{goodQuantity},
            "総不良品数" = "総不良品数" + #{defectQuantity},
            "更新日時" = CURRENT_TIMESTAMP,
            "バージョン" = "バージョン" + 1
        WHERE "作業指示番号" = #{workOrderNumber}
          AND "バージョン" = #{version}
    </update>

    <!-- PostgreSQL用 DELETE -->
    <delete id="deleteAll" databaseId="postgresql">
        TRUNCATE TABLE "作業指示データ" CASCADE
    </delete>

    <!-- H2用 DELETE -->
    <delete id="deleteAll" databaseId="h2">
        DELETE FROM "作業指示データ"
    </delete>
</mapper>
