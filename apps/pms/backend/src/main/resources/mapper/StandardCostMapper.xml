<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pms.infrastructure.out.persistence.mapper.StandardCostMapper">

    <resultMap id="standardCostResultMap" type="com.example.pms.domain.model.cost.StandardCost">
        <id property="id" column="ID"/>
        <result property="itemCode" column="品目コード"/>
        <result property="effectiveStartDate" column="適用開始日"/>
        <result property="effectiveEndDate" column="適用終了日"/>
        <result property="standardMaterialCost" column="標準材料費"/>
        <result property="standardLaborCost" column="標準労務費"/>
        <result property="standardExpense" column="標準経費"/>
        <result property="standardManufacturingCost" column="標準製造原価"/>
        <result property="version" column="バージョン"/>
        <result property="createdAt" column="作成日時"/>
        <result property="updatedAt" column="更新日時"/>
    </resultMap>

    <select id="findById" resultMap="standardCostResultMap">
        SELECT * FROM "標準原価マスタ" WHERE "ID" = #{id}
    </select>

    <select id="findByItemCode" resultMap="standardCostResultMap">
        SELECT * FROM "標準原価マスタ"
        WHERE "品目コード" = #{itemCode}
        ORDER BY "適用開始日" DESC
    </select>

    <select id="findValidByItemCode" resultMap="standardCostResultMap">
        SELECT * FROM "標準原価マスタ"
        WHERE "品目コード" = #{itemCode}
          AND "適用開始日" &lt;= #{targetDate}
          AND ("適用終了日" IS NULL OR "適用終了日" >= #{targetDate})
        ORDER BY "適用開始日" DESC
        LIMIT 1
    </select>

    <select id="findAll" resultMap="standardCostResultMap">
        SELECT * FROM "標準原価マスタ" ORDER BY "品目コード", "適用開始日" DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="ID">
        INSERT INTO "標準原価マスタ" (
            "品目コード", "適用開始日", "適用終了日",
            "標準材料費", "標準労務費", "標準経費", "標準製造原価"
        ) VALUES (
            #{itemCode}, #{effectiveStartDate}, #{effectiveEndDate},
            #{standardMaterialCost}, #{standardLaborCost},
            #{standardExpense}, #{standardManufacturingCost}
        )
    </insert>

    <update id="update">
        UPDATE "標準原価マスタ"
        SET "適用終了日" = #{effectiveEndDate},
            "標準材料費" = #{standardMaterialCost},
            "標準労務費" = #{standardLaborCost},
            "標準経費" = #{standardExpense},
            "標準製造原価" = #{standardManufacturingCost},
            "更新日時" = CURRENT_TIMESTAMP,
            "バージョン" = "バージョン" + 1
        WHERE "ID" = #{id}
          AND "バージョン" = #{version}
    </update>

    <delete id="deleteById">
        DELETE FROM "標準原価マスタ" WHERE "ID" = #{id}
    </delete>

    <delete id="deleteByItemCode">
        DELETE FROM "標準原価マスタ" WHERE "品目コード" = #{itemCode}
    </delete>

    <delete id="deleteAll">
        DELETE FROM "標準原価マスタ"
    </delete>

</mapper>
