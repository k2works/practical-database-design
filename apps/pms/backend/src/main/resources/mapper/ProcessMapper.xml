<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pms.infrastructure.out.persistence.mapper.ProcessMapper">

    <resultMap id="ProcessResultMap" type="com.example.pms.domain.model.process.Process">
        <id property="processCode" column="工程コード"/>
        <result property="processName" column="工程名"/>
        <result property="processType" column="工程区分"/>
        <result property="locationCode" column="場所コード"/>
        <result property="createdAt" column="作成日時"/>
        <result property="updatedAt" column="更新日時"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.pms.domain.model.process.Process">
        INSERT INTO "工程マスタ" (
            "工程コード", "工程名", "工程区分", "場所コード", "作成日時", "更新日時"
        ) VALUES (
            #{processCode}, #{processName}, #{processType}, #{locationCode},
            CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
        )
    </insert>

    <select id="findByProcessCode" resultMap="ProcessResultMap">
        SELECT * FROM "工程マスタ"
        WHERE "工程コード" = #{processCode}
    </select>

    <select id="findAll" resultMap="ProcessResultMap">
        SELECT * FROM "工程マスタ"
        ORDER BY "工程コード"
    </select>

    <update id="update" parameterType="com.example.pms.domain.model.process.Process">
        UPDATE "工程マスタ" SET
            "工程名" = #{processName},
            "工程区分" = #{processType},
            "場所コード" = #{locationCode},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "工程コード" = #{processCode}
    </update>

    <delete id="deleteAll" databaseId="postgresql">
        TRUNCATE TABLE "工程マスタ" CASCADE
    </delete>

    <delete id="deleteAll" databaseId="h2">
        DELETE FROM "工程マスタ"
    </delete>
</mapper>
