<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pms.infrastructure.out.persistence.mapper.RequirementMapper">

    <!-- 日本語カラム名と英語プロパティ名のマッピング -->
    <resultMap id="RequirementResultMap" type="com.example.pms.domain.model.plan.Requirement">
        <id property="id" column="ID"/>
        <result property="requirementNumber" column="所要NO"/>
        <result property="orderId" column="オーダID"/>
        <result property="itemCode" column="品目コード"/>
        <result property="dueDate" column="納期"/>
        <result property="requiredQuantity" column="必要数量"/>
        <result property="allocatedQuantity" column="引当済数量"/>
        <result property="shortageQuantity" column="不足数量"/>
        <result property="locationCode" column="場所コード"/>
        <result property="createdAt" column="作成日時"/>
        <result property="updatedAt" column="更新日時"/>
        <result property="version" column="バージョン"/>
    </resultMap>

    <!-- 引当を含むResultMap（nested select） -->
    <resultMap id="RequirementWithAllocationsResultMap" type="com.example.pms.domain.model.plan.Requirement"
               extends="RequirementResultMap">
        <collection property="allocations" ofType="com.example.pms.domain.model.plan.Allocation"
                    column="ID" select="com.example.pms.infrastructure.out.persistence.mapper.AllocationMapper.findByRequirementId"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.pms.domain.model.plan.Requirement"
            useGeneratedKeys="true" keyProperty="id" keyColumn="ID">
        INSERT INTO "所要情報" (
            "所要NO", "オーダID", "品目コード", "納期", "必要数量",
            "引当済数量", "不足数量", "場所コード"
        ) VALUES (
            #{requirementNumber},
            #{orderId},
            #{itemCode},
            #{dueDate},
            #{requiredQuantity},
            #{allocatedQuantity},
            #{shortageQuantity},
            #{locationCode}
        )
    </insert>

    <select id="findById" resultMap="RequirementResultMap">
        SELECT * FROM "所要情報" WHERE "ID" = #{id}
    </select>

    <select id="findByRequirementNumber" resultMap="RequirementResultMap">
        SELECT * FROM "所要情報" WHERE "所要NO" = #{requirementNumber}
    </select>

    <!-- 引当を含めて取得 -->
    <select id="findByRequirementNumberWithAllocations" resultMap="RequirementWithAllocationsResultMap">
        SELECT * FROM "所要情報" WHERE "所要NO" = #{requirementNumber}
    </select>

    <select id="findByOrderId" resultMap="RequirementResultMap">
        SELECT * FROM "所要情報" WHERE "オーダID" = #{orderId} ORDER BY "納期"
    </select>

    <select id="findAll" resultMap="RequirementResultMap">
        SELECT * FROM "所要情報" ORDER BY "納期"
    </select>

    <update id="updateAllocation">
        UPDATE "所要情報"
        SET "引当済数量" = #{allocatedQuantity},
            "不足数量" = #{shortageQuantity},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "ID" = #{id}
    </update>

    <!-- PostgreSQL用 DELETE -->
    <delete id="deleteAll" databaseId="postgresql">
        TRUNCATE TABLE "所要情報" CASCADE
    </delete>

    <!-- H2用 DELETE -->
    <delete id="deleteAll" databaseId="h2">
        DELETE FROM "所要情報"
    </delete>
</mapper>
