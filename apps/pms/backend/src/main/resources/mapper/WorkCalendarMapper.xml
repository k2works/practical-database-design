<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pms.infrastructure.out.persistence.mapper.WorkCalendarMapper">

    <!-- 日本語カラム名と英語プロパティ名のマッピング -->
    <resultMap id="WorkCalendarResultMap" type="com.example.pms.domain.model.calendar.WorkCalendar">
        <id property="calendarCode" column="カレンダコード"/>
        <id property="date" column="日付"/>
        <result property="dateType" column="日付区分"
                typeHandler="com.example.pms.infrastructure.out.persistence.typehandler.DateTypeTypeHandler"/>
        <result property="workingHours" column="稼働時間"/>
        <result property="note" column="備考"/>
        <result property="createdAt" column="作成日時"/>
        <result property="updatedAt" column="更新日時"/>
    </resultMap>

    <!-- PostgreSQL用 INSERT -->
    <insert id="insert" parameterType="com.example.pms.domain.model.calendar.WorkCalendar" databaseId="postgresql">
        INSERT INTO "カレンダマスタ" (
            "カレンダコード",
            "日付",
            "日付区分",
            "稼働時間",
            "備考",
            "作成日時",
            "更新日時"
        ) VALUES (
            #{calendarCode},
            #{date},
            #{dateType, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.DateTypeTypeHandler}::日付区分,
            #{workingHours},
            #{note},
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP
        )
    </insert>

    <!-- H2用 INSERT -->
    <insert id="insert" parameterType="com.example.pms.domain.model.calendar.WorkCalendar" databaseId="h2">
        INSERT INTO "カレンダマスタ" (
            "カレンダコード",
            "日付",
            "日付区分",
            "稼働時間",
            "備考",
            "作成日時",
            "更新日時"
        ) VALUES (
            #{calendarCode},
            #{date},
            #{dateType, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.DateTypeTypeHandler},
            #{workingHours},
            #{note},
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP
        )
    </insert>

    <select id="findByCalendarCodeAndDate" resultMap="WorkCalendarResultMap">
        SELECT * FROM "カレンダマスタ"
        WHERE "カレンダコード" = #{calendarCode}
          AND "日付" = #{date}
    </select>

    <select id="findByCalendarCode" resultMap="WorkCalendarResultMap">
        SELECT * FROM "カレンダマスタ"
        WHERE "カレンダコード" = #{calendarCode}
        ORDER BY "日付"
    </select>

    <select id="findByCalendarCodeAndDateRange" resultMap="WorkCalendarResultMap">
        SELECT * FROM "カレンダマスタ"
        WHERE "カレンダコード" = #{calendarCode}
          AND "日付" >= #{startDate}
          AND "日付" &lt;= #{endDate}
        ORDER BY "日付"
    </select>

    <!-- PostgreSQL用 UPDATE -->
    <update id="update" parameterType="com.example.pms.domain.model.calendar.WorkCalendar" databaseId="postgresql">
        UPDATE "カレンダマスタ" SET
            "日付区分" = #{dateType, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.DateTypeTypeHandler}::日付区分,
            "稼働時間" = #{workingHours},
            "備考" = #{note},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "カレンダコード" = #{calendarCode}
          AND "日付" = #{date}
    </update>

    <!-- H2用 UPDATE -->
    <update id="update" parameterType="com.example.pms.domain.model.calendar.WorkCalendar" databaseId="h2">
        UPDATE "カレンダマスタ" SET
            "日付区分" = #{dateType, typeHandler=com.example.pms.infrastructure.out.persistence.typehandler.DateTypeTypeHandler},
            "稼働時間" = #{workingHours},
            "備考" = #{note},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "カレンダコード" = #{calendarCode}
          AND "日付" = #{date}
    </update>

    <!-- PostgreSQL用 DELETE -->
    <delete id="deleteAll" databaseId="postgresql">
        TRUNCATE TABLE "カレンダマスタ" CASCADE
    </delete>

    <!-- H2用 DELETE -->
    <delete id="deleteAll" databaseId="h2">
        DELETE FROM "カレンダマスタ"
    </delete>
</mapper>
