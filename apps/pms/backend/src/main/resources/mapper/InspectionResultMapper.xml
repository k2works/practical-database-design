<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pms.infrastructure.out.persistence.mapper.InspectionResultMapper">

    <resultMap id="InspectionResultResultMap" type="com.example.pms.domain.model.process.InspectionResult">
        <id property="id" column="ID"/>
        <result property="completionResultNumber" column="完成実績番号"/>
        <result property="defectCode" column="欠点コード"/>
        <result property="quantity" column="数量"/>
        <result property="createdAt" column="作成日時"/>
        <result property="createdBy" column="作成者"/>
        <result property="updatedAt" column="更新日時"/>
        <result property="updatedBy" column="更新者"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.pms.domain.model.process.InspectionResult"
            useGeneratedKeys="true" keyProperty="id" keyColumn="ID">
        INSERT INTO "完成検査結果データ" (
            "完成実績番号", "欠点コード", "数量", "作成者", "更新者"
        ) VALUES (
            #{completionResultNumber},
            #{defectCode},
            #{quantity},
            #{createdBy},
            #{updatedBy}
        )
    </insert>

    <select id="findById" resultMap="InspectionResultResultMap">
        SELECT * FROM "完成検査結果データ" WHERE "ID" = #{id}
    </select>

    <select id="findByCompletionResultNumberAndDefectCode" resultMap="InspectionResultResultMap">
        SELECT * FROM "完成検査結果データ"
        WHERE "完成実績番号" = #{completionResultNumber} AND "欠点コード" = #{defectCode}
    </select>

    <select id="findByCompletionResultNumber" resultMap="InspectionResultResultMap">
        SELECT * FROM "完成検査結果データ"
        WHERE "完成実績番号" = #{completionResultNumber}
        ORDER BY "欠点コード"
    </select>

    <select id="findAll" resultMap="InspectionResultResultMap">
        SELECT * FROM "完成検査結果データ" ORDER BY "完成実績番号", "欠点コード"
    </select>

    <!-- PostgreSQL用 DELETE -->
    <delete id="deleteAll" databaseId="postgresql">
        TRUNCATE TABLE "完成検査結果データ" CASCADE
    </delete>

    <!-- H2用 DELETE -->
    <delete id="deleteAll" databaseId="h2">
        DELETE FROM "完成検査結果データ"
    </delete>
</mapper>
